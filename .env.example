# ────────────────────────────────
#  Web Technologies 2 - Example Environment File
# ────────────────────────────────

# PostgreSQL configuration
POSTGRES_USER=student
POSTGRES_PASSWORD=devpass
POSTGRES_DB=webtech2db
POSTGRES_PORT=5432

# pgAdmin configuration
PGADMIN_DEFAULT_EMAIL=student@example.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT=5050

# Application server
PORT=3000

# Database connection URL (for use in Node/Prisma/etc.)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# JWT or session secret (used in later assignments)
JWT_SECRET=supersecretkey

